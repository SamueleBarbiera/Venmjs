'use strict'
import showBanner from 'node-banner'

/**
 * @returns {Void}
 * @param {String} appName 
 * @returns {Promise<void>}
 */

export default async (appName) => {
    await showBanner('BACKEND   SECTION   NOT   READY','-------------------------------------------------------------------------------','red', 'red' )
}
